{
  "name": "sovereign-terminal-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Private STOS gateway with one-use Wix-brokered tickets, SSH/tmux profiles, and disposable TEST sandbox.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "check": "node --check src/server.mjs && node --check src/config.mjs && node --check src/tickets.mjs && node --check src/sessions.mjs"
  },
  "dependencies": {
    "@xterm/xterm": "6.0.0",
    "node-pty": "^1.0.0",
    "ssh2": "^1.16.0",
    "ws": "^8.18.0"
  }
}
