terminal.example.com {
  encode zstd gzip

  reverse_proxy 127.0.0.1:8787

  header {
    -Server
  }

  # Ticket values are not placed in URLs, so normal path logging does not record them.
  log {
    output file /var/log/caddy/stos-access.log
    format json
  }
}
